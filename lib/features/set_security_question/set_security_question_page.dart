import 'package:flutter/material.dart';

class SetSecurityQuestionPage extends StatelessWidget {
  const SetSecurityQuestionPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
