import 'package:flutter/material.dart';

class SubNavigationWidget extends StatelessWidget {
  const SubNavigationWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Sub-navigation");
  }
}
